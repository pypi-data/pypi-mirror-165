import ReactDOM from 'react-dom';
export var createNode = function (children, className) {
    var mountPoint = document.createElement('div');
    mountPoint.className = className;
    ReactDOM.render(children, mountPoint);
    return mountPoint;
};
