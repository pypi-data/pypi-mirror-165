[tool.poetry]
name = "beam-cli"
version = "0.1.0"
description = ""
authors = ["Luke Lombardi <luke@slai.io>"]
packages = [
    { include = "beam" },
    { include = "beam/**/*.py" },
]

[tool.poetry.dependencies]
python = ">=3.7,<4.0.0"
typer = "^0.6.1"
yamldataclassconfig = "^1.5.0"
rich = "^12.5.1"
websockets = "^10.3"
requests = "^2.28.1"
python-syncthing-client = "^0.1.1"

[tool.poetry.dev-dependencies]
black = "^22.6.0"
flake8 = "^5.0.4"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
beam = "beam.cli:app"

