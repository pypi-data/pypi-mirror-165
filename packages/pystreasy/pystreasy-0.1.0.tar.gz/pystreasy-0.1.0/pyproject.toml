[tool.poetry]
name = "pystreasy"
version = "0.1.0"
description = "makes working with strings easy"
repository = "https://github.com/ablesoftwaredev/pystreasy"
authors = [
    "ablesoftwaredev <48694187+ablesoftwaredev@users.noreply.github.com>"
]

[tool.poetry.dependencies]
python = "^3.10"

[tool.poetry.dev-dependencies]
black = "^22.6.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
