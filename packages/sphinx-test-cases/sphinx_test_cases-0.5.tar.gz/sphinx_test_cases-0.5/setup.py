from setuptools import setup, find_packages

# read the contents of your README file
from pathlib import Path
this_directory = Path(__file__).parent
long_description = (this_directory / "README.md").read_text()

setup(
    name='sphinx_test_cases',
    version='0.5',
    license='Apache',
    author="Arturim",
    author_email='lozinski.artur@gmail.com',
    description=(
        """This package is a simple extention to the Sphinx, which is
        dedicated to describe test cases. Includes also a directives to
        autogenerated from the python function description"""),
    packages=find_packages('src'),
    package_dir={'': 'src'},
    url='https://github.com/ArtUrim/sphinx-test',
    keywords='sphinx test case scenario plan',
    long_description=long_description,
    long_description_content_type='text/markdown',
    install_requires=[
          'sphinx',
          'wheel',
      ],

)
