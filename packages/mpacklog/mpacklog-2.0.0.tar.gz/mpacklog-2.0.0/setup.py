#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['mpacklog', 'mpacklog.cli', 'mpacklog.python']

package_data = \
{'': ['*'],
 'mpacklog': ['cpp/*', 'cpp/tests/*'],
 'mpacklog.python': ['tests/*']}

install_requires = \
['aiofiles >=0.7.0', 'msgpack >=1.0.2']

extras_require = \
{'doc': ['sphinx']}

entry_points = \
{'console_scripts': ['mpacklog = mpacklog.cli:main']}

setup(name='mpacklog',
      version='2.0.0',
      description='Log dictionaries to file using the MessagePack serialization format.',
      author=None,
      author_email='Stéphane Caron <stephane.caron@normalesup.org>',
      url=None,
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      extras_require=extras_require,
      entry_points=entry_points,
      python_requires='>=3.7',
     )
