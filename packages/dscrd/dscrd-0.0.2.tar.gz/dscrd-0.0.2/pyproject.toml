[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "dscrd"
description = "Fast and easy to use API Wrapper for Discord."
version = "0.0.2"
dependencies = [
    "requests",
    "websockets",
    "pynacl"
]